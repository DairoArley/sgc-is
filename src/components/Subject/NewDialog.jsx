import { DialogTitle as _tittle} from '@mui/material';
import {styled } from '@material-ui/styles';

const NewDialog = styled(_tittle)`
    fontWeight: 'bold',
    fontSize: '1.5rem',
    color: '#09612d',
    marginBottom: '1rem',
`;

export default NewDialog;